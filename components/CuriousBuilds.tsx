export function CuriousBuilds() {
  return (
    <section id="lab" className="mx-auto max-w-5xl px-6 py-20 sm:px-8 sm:py-28">
      <p className="font-hero text-xs tracking-widest text-muted uppercase">
        Curious Builds
      </p>
      <p className="mt-4 max-w-xl font-hero text-lg leading-relaxed text-muted">
        This is where things I build to explore a question will live —
        GitHub projects, prototypes, AI experiments, course projects.
      </p>
      <p className="mt-4 font-hero text-sm text-muted">
        Projects coming here as I build them.
      </p>
    </section>
  );
}
